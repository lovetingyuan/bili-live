{
  "name": "check-bili-live",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "start": "wrangler dev --test-scheduled",
    "cf-typegen": "wrangler types",
    "test": "npx wrangler dev --port=8788 --test-scheduled"
  },
  "devDependencies": {
    "@types/node": "^22.16.4",
    "typescript": "^5.8.3",
    "wrangler": "^4.106.0"
  },
  "dependencies": {
    "arktype": "^2.1.20"
  }
}
